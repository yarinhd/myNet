import { styled } from '@mui/material';

export const IntroductionDiv = styled('div')({
    gap: '1.5rem',
    display: 'flex',
    flexDirection: 'column',
    justifyContent: 'flex-start',
});

export const DontKnowSpawn = styled('span')({
    backgroundColor: '#E0E0E0',
    fontSize: '0.8rem',
    padding: '0.5rem',
    borderRadius: '5px',
    marginRight: '0.5rem',
});
