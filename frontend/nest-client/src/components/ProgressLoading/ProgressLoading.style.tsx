import { Grid, styled } from '@mui/material';

const LoadingWarp = styled(Grid)({
    width: '100%',
    height: '100%',
    justifyContent: 'center',
    alignItems: 'center',
});

export default LoadingWarp;
